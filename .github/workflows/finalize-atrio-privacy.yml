name: Finalize ATRIO privacy cleanup

on:
  push:
    paths:
      - '.github/workflows/finalize-atrio-privacy.yml'
      - 'tools/finalize_atrio_privacy.py'

permissions:
  contents: write

jobs:
  finalize:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout main
        uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0

      - name: Apply ATRIO cleanup
        run: python tools/finalize_atrio_privacy.py

      - name: Commit validated cleanup
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          if git diff --cached --quiet; then
            echo "No changes to commit"
            exit 0
          fi
          git commit -m "Remove residual peer benchmarks from ATRIO"
          git push origin HEAD:main
