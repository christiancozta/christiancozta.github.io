
# acervo cru (nao versionar - dado pessoal, LGPD)
2a_turma_*.json

# dependencias de ferramenta (nao ha package.json; vite roda por npx)
node_modules/

# estado de execucao do live server (pid/token)
.impeccable/
